# proxy/requirements-server.txt — VM-side dependencies
# Install with: pip install -r proxy/requirements-server.txt

# Web server
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1

# Pipeline dependencies (mirrors project requirements.txt)
numpy>=2.0,<3
shapely>=2.0,<3
pyproj>=3.6,<4
rasterio==1.4.4
polars>=1.0,<2
pyarrow>=15,<25
duckdb>=1.0,<2
requests>=2.31,<3
pyyaml>=6.0,<7
pystac-client>=0.7,<0.9
httpx>=0.27,<1

# S1 signing
azure-storage-blob>=12,<13
