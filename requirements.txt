# Core geospatial
rioxarray>=0.15,<0.17
scipy>=1.12,<2
pysheds>=0.4,<1
xarray>=2024.1,<2026
numpy>=2.0,<3
shapely>=2.0,<3
pyproj>=3.6,<4

# Raster I/O
rasterio==1.4.4

# EO data loading
sarsen>=0.9,<1
stackstac>=0.5,<0.6
odc-stac>=0.3,<0.4
odc-geo>=0.4,<0.5
pystac-client>=0.7,<0.9

# Machine learning
scikit-learn>=1.4,<2
# Blackwell GPU (RTX 5060 Ti, sm_120) requires cu130 — install separately:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch>=2.7
# Flash attention for varlen inference (no padding waste). Pre-built wheel for CUDA 13 / Python 3.14:
#   pip install "https://github.com/Dao-AILab/flash-attention/releases/download/fa4-v4.0.0.beta4/flash_attn_4-4.0.0b4-py3-none-any.whl"
flash-attn-4

# Parallel computation
dask[array,distributed]>=2024.1,<2024.10
polars>=1.0,<2
pyarrow>=15,<25
duckdb>=1.0,<2
adlfs>=2024.2

# setuptools is required at runtime by stackstac (pkg_resources)
setuptools>=69,<81

# HTTP
requests>=2.31,<3

# Visualization
matplotlib>=3.8,<4

# CLI progress bars (cli/chunk.py verify)
tqdm>=4.66,<5

# Configuration
pyyaml>=6.0,<7
python-dotenv>=1.0,<2
